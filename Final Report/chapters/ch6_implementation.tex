\chapter{Implementation Details}
\label{ch:progress}

This chapter documents the implementation status and key engineering decisions made during development.

\section{Overall Implementation Status}
\label{sec:impl_status}

\noindent\textbf{Live Deployment:} The system is currently deployed at \url{http://techpulse-dev-frontend-939514668437.s3-website-us-east-1.amazonaws.com/}.

Table~\ref{tab:impl_status} summarizes the implementation status of each system component as of the reporting date.

\noindent\textbf{Status key:} \textit{Unit-tested} = implemented and verified by unit tests only; \textit{Integration-tested} = verified end-to-end in local Docker Compose environment; \textit{Cloud-validated} = deployed and verified in the AWS Free Tier.

\begin{table}[htbp]
\caption{Component Implementation Status Summary.}
\label{tab:impl_status}
\centering
\begin{tabular}{p{4.2cm}p{2.8cm}p{5.5cm}}
\toprule
\textbf{Component} & \textbf{Status} & \textbf{Notes} \\
\midrule
Data Ingestion (5 sources) & Integration-tested & ArXiv, Hacker News, DEV.to, GitHub, RSS fully implemented \\
Preprocessing Pipeline & Integration-tested & 17-step normalization + token-based chunking \\
Embedding Pipeline & Integration-tested & MiniLM L6 v2 (384-dim), lazy singleton \\
Hybrid Retrieval & Cloud-validated & 5-stage pipeline (metadata filter, vector, BM25, weighted RRF, cross-encoder) \\
Baseline Retrieval & Integration-tested & Vector-only comparison mode \\
RAG Orchestrator & Integration-tested & 3-layer hallucination verification \\
LLM Backend Fallback & Unit-tested & Auto-cascade across 4 backends \\
API Layer & Integration-tested & FastAPI + Mangum; /health, /ask, /drift endpoints \\
Database Schema & Integration-tested & PostgreSQL + pgvector with HNSW index \\
S3 Medallion Storage & Integration-tested & Raw/processed/embeddings tiers (LocalStack locally) \\
SQS Queue + DLQ & Integration-tested & Ingestion--embedding decoupling (LocalStack locally) \\
Drift Detection & Unit-tested & Dual-criteria: 10\% threshold + Shewhart $3\sigma$ \\
Health Monitoring & Integration-tested & 4-probe deep health check (DB, S3, SQS, LLM) \\
CloudWatch Integration & Cloud-validated & 8 metrics in 1 namespace (\texttt{TechPulse/\{stage\}}) \\
Evaluation Framework & Cloud-validated & 100-sample run (50 baseline + 50 hybrid), RAGAS, statistical tests, and cost projection; follow-on drift-impact analysis remains future work \\
CI/CD Pipeline & Cloud-validated & 4-stage GitHub Actions + SAM \\
SAM Infrastructure & Cloud-validated & Full SAM template; stack deployed and operational \\
Docker Compose (Local) & Integration-tested & 6 services (db, pgadmin, api, frontend, scheduler, localstack) \\
React Frontend & Integration-tested & Query interface with citation display \\
Unit Tests & Integration-tested & 208 tests across 15 test files, all passing \\
\bottomrule
\end{tabular}
\end{table}

\section{Key Enhancements Beyond the Original Proposal}
\label{sec:enhancements}

Two significant enhancements were made beyond the original proposal: (1) data source coverage was expanded from two to five sources (ArXiv, Hacker News, DEV.to, GitHub Trending, RSS feeds) to improve corpus diversity and temporal coverage; and (2) the preprocessing pipeline was extended from basic HTML stripping to a 17-step normalization pipeline (validated by 18 dedicated unit tests) to handle the diverse content formats encountered across sources, particularly Markdown-heavy content from DEV.to and GitHub. All five ingesters share the same idempotent, SHA-256-based deduplication architecture.

Additionally, nine research-backed retrieval improvements were applied post-proposal, including: weighted Reciprocal Rank Fusion (vector~0.50, BM25~0.35, recency~0.15), improved BM25 tokenization with stop-word removal, BM25-only candidate quality gate, asymmetric candidate limits, post-fusion score gate, Shewhart $3\sigma$ minimum standard deviation floor for drift detection, RAGAS evaluation threading fix, cross-encoder reranking (ms-marco-MiniLM-L-6-v2), and NaN retry pass for RAGAS scoring. These improvements are documented in the project's engineering change log and collectively produced a composite quality swing of $+0.069$---from trailing the baseline by $-0.022$ to leading by $+0.047$.

\section{AWS Free Tier Deployment}
\label{sec:freetier}

TechPulse is deployed on a personal AWS Free Tier account. The deployment adapts certain services from the original proposal to remain within Free Tier cost constraints while preserving full architectural capabilities. 

\subsection*{Cost Breakdown (100 queries/day estimate)}

Table~\ref{tab:cost_breakdown} provides a component-level cost estimate for the anticipated 100 queries/day operational scenario:

\begin{table}[H]
\centering
\caption{Estimated Monthly Cost Breakdown at 100 Queries/Day Workload.}
\label{tab:cost_breakdown}
\begin{tabular}{p{3.2cm}p{2.0cm}p{3.5cm}p{2.0cm}}
\toprule
\textbf{Component} & \textbf{Cost/Month} & \textbf{Details} & \textbf{Free Tier} \\
\midrule
Lambda (ingestion) & \$0.05 & 10 short-lived invocations/day & \checkmark \\
Lambda (query handler) & \$0.08 & 100 queries/day, 3.1s avg & \checkmark \\
Groq LLM API & \$0.12\textsuperscript{$\dagger$} & $\sim$2,300 tokens/query average & Free (500k/day) \\
RDS PostgreSQL & \$0.08 & db.t3.micro instance (micro is free) & \checkmark \\
S3 + EventBridge + SQS & \$0.00 & Storage, routing, queue services & \checkmark \\
\midrule
\textbf{Total Monthly} & \textbf{\$0.33} & \textbf{Extrapolated from Phase B eval} & \textbf{\checkmark} \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textsuperscript{$\dagger$}Groq inference is free within the 500k tokens/day allowance; \$0.12 represents the imputed cost at equivalent commercial per-token rates to enable fair cost projection if the free tier were unavailable.

\noindent Costs are derived from observed token usage and Lambda invocation metrics collected during the evaluation run. At higher workloads exceeding the Groq free-tier allowance (500,000 tokens/day), the automatic LLM backend fallback chain (Bedrock → Ollama → HuggingFace) enables cost-controlled scaling. The monthly total of \$0.33 is well below the \$15 budget target, providing a 45$\times$ safety margin.

\subsection*{Key Deployment Characteristics}

The key deployment characteristics are:

\begin{itemize}[leftmargin=0.5in]
  \item \textbf{RDS PostgreSQL instead of Aurora Serverless.} Standard RDS PostgreSQL \texttt{db.t3.micro} (PostgreSQL 16.6, 20GB gp2) replaces the originally proposed Aurora Serverless to stay within budget. The pgvector extension and HNSW indexing remain fully functional.
  \item \textbf{Groq as primary LLM backend.} Groq free-tier API (Llama 3.1 8B Instant) serves as the primary backend, with Amazon Bedrock (Nova Micro), Ollama, and HuggingFace as fallbacks. The auto-fallback chain ensures generation availability.
  \item \textbf{Default VPC with security groups.} The SAM template uses the default VPC with two subnets. RDS is configured as publicly accessible with security group restrictions; custom VPC migration is identified as a future improvement.
  \item \textbf{Fine-grained IAM roles.} Custom IAM role definitions provide least-privilege access for each Lambda function.
  \item \textbf{AWS Budgets alerts.} Native AWS Budgets alerts provide real-time cost governance, supplemented by programmatic Cost Explorer checks in the inference path.
  \item \textbf{LLM backend set to \texttt{groq}.} The \texttt{LLM\_BACKEND} environment variable defaults to \texttt{groq}, with Amazon Bedrock and Ollama retained as fallbacks.
\end{itemize}

All infrastructure is defined in a SAM template (\texttt{template-freetier.yaml}) and configuration file (\texttt{samconfig-freetier.toml}), ensuring reproducible and consistent deployments from the same codebase.

\section{Test Coverage}
\label{sec:tests}

The system includes 208 unit tests distributed across 15 test modules covering all major subsystems: ingestion (5 sources), preprocessing (17 normalization steps), embedding, retrieval (BM25 caching, RRF fusion, age-days computation, cross-encoder reranking), RAG orchestration, LLM backend fallback, observability (CloudWatch metrics, drift detection), pipeline orchestration, queue management, storage, API endpoints (including /dashboard/insights smoke tests), and scheduler. All tests execute without external dependencies (database, AWS services) using mocked interfaces where appropriate.

\section{Custom CloudWatch Metrics}
\label{sec:metrics}

Eight custom CloudWatch metrics have been implemented within a single \texttt{TechPulse/\{stage\}} namespace to provide operational visibility:

\begin{enumerate}[leftmargin=0.5in]
  \item \texttt{IngestionCount} --- documents ingested per source per cycle
  \item \texttt{PipelineChunks} --- chunks generated per pipeline run
  \item \texttt{PipelineLatency} --- end-to-end pipeline execution time
  \item \texttt{PipelineErrors} --- pipeline failure count
  \item \texttt{ApiLatency} --- per-request API response time
  \item \texttt{HallucinationFlags} --- count of citation grounding failures
\end{enumerate}

Additionally, the drift detection module publishes \texttt{DriftMeanSimilarity} and \texttt{DriftAlert} metrics for time-series tracking of retrieval quality.

\section{Engineering Observations}
\label{sec:eng_obs}

Several non-obvious findings arose during implementation that informed the final design:

\begin{itemize}[leftmargin=0.5in]
  \item \textbf{Lambda cold-start and MiniLM.} Initial cold-start delays of 8--12\,s were resolved by a lazy-initialized singleton and 1{,}024\,MB memory allocation, reducing model load to $<$2\,s on warm invocations.

  \item \textbf{SHA-256 deduplication.} The \texttt{ON CONFLICT DO NOTHING} strategy proved fully effective; ArXiv produced the most hash collisions ($\sim$15--20\% per cycle) due to preprint updates.

  \item \textbf{LocalStack fidelity.} S3 and SQS operations matched production behavior, enabling integration testing without AWS credentials. CloudWatch metrics required cloud deployment to verify.

  \item \textbf{HuggingFace rate limits.} The free-tier rate limit ($\sim$30 req/min) triggered the Ollama fallback during sustained evaluation runs, confirming the fallback chain operates correctly.

  \item \textbf{pgvector HNSW index build.} Index rebuilds took 45--90\,s on db.t3.micro for corpora above $\sim$10k chunks---acceptable for batch but would need management for real-time updates.
\end{itemize}

\section{System Demonstration}
\label{sec:demo}

This section presents screenshots from the deployed system, demonstrating end-to-end functionality across key components.

\subsection{Frontend: Overview Page}

Figure~\ref{fig:demo_overview} shows the TechPulse landing page, providing the query input interface with retrieval mode selection (Baseline or Hybrid) and the system branding.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{techpulse-overviewpage.png}
\caption{TechPulse frontend: overview page with query input and retrieval mode selection.}
\label{fig:demo_overview}
\end{figure}

\subsection{Frontend: Hybrid vs.\ Baseline Retrieval Comparison}

Figure~\ref{fig:demo_compare} shows the same query executed in both retrieval modes. The hybrid response (left) includes diverse, multi-source citations fused via RRF, while the baseline response (right) relies solely on vector similarity. This figure is intended as a qualitative interface illustration; quantitative retrieval conclusions are based on the statistical results reported in Chapter~5.

\begin{figure}[htbp]
\centering
\begin{subfigure}[t]{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{techpulse-frontend-hybrid.png}
\caption{Hybrid retrieval (BM25 + vector + RRF).}
\label{fig:demo_hybrid}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
\centering
\includegraphics[width=\textwidth]{techpulse-frontend-baseline.png}
\caption{Baseline retrieval (vector-only).}
\label{fig:demo_baseline}
\end{subfigure}
\caption{TechPulse frontend: hybrid vs.\ baseline retrieval for the same query.}
\label{fig:demo_compare}
\end{figure}

\subsection{CI/CD Pipeline: GitHub Actions}

Figure~\ref{fig:demo_cicd} shows the GitHub Actions CI/CD pipeline executing the workflow, validating that continuous integration and deployment are fully operational.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{github workflow run.png}
\caption{GitHub Actions CI/CD pipeline: workflow run executing successfully.}
\label{fig:demo_cicd}
\end{figure}

\subsection{AWS CloudFormation Stack}

Figure~\ref{fig:demo_cfn} shows the deployed AWS CloudFormation stack created by the SAM template, confirming that all infrastructure resources (Lambda functions, API Gateway, S3 buckets, SQS queues, EventBridge rules, and IAM roles) are provisioned and operational.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.82\textwidth]{techpulse-cloudformation.png}
\caption{AWS CloudFormation console: TechPulse stack with all resources successfully created.}
\label{fig:demo_cfn}
\end{figure}

\subsection{Database: RDS PostgreSQL with Ingested Corpus}

Figure~\ref{fig:demo_rds} shows the AWS RDS console with the TechPulse PostgreSQL instance, confirming that the production database is running and accessible for the ingestion pipeline and retrieval queries.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.82\textwidth]{rds-techpulse.png}
\caption{AWS RDS console: TechPulse PostgreSQL instance with pgvector extension.}
\label{fig:demo_rds}
\end{figure}

\medskip
\noindent This chapter documented implementation status across 20 components and key engineering observations. All core pipeline components are integration-tested; cloud deployment via SAM is validated. Chapter~7 presents conclusions, limitations, and future work.

