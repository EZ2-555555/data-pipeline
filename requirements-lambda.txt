# Runtime dependencies for Lambda function code.
# boto3 is provided by the Lambda runtime — do not bundle it.

# Core
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0
defusedxml>=0.7.1

# Embeddings (ONNX-based — ~150-200 MB with onnxruntime; deployed via container image)
fastembed>=0.4.0

# Tokenisation
tiktoken>=0.5.0

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# API
fastapi>=0.109.0
mangum>=0.17.0
slowapi>=0.1.9

# Retrieval
rank-bm25>=0.2.2
