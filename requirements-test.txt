# Lightweight requirements for CI lint + unit tests.
# Tests mock all heavy dependencies (torch, sentence-transformers),
# so we only need the import-time dependencies here.
# sentence_transformers is lazy-imported in embedder.py, so torch
# is NOT needed for test collection or execution.

# Core
boto3>=1.34.0
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0
defusedxml>=0.7.1

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# API
fastapi>=0.109.0
uvicorn>=0.27.0
mangum>=0.17.0
slowapi>=0.1.9

# NLP (lightweight — tokenizer only)
tiktoken>=0.5.0

# AWS
aws-lambda-powertools>=2.30.0

# Testing and Linting
pytest>=7.4.0
ruff>=0.3.0
pytest-cov>=4.1.0
