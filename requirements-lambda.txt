# Runtime-only dependencies for AWS Lambda (CPU environment).
# NOTE: torch is installed separately in the Makefile to guarantee
# the CPU-only wheel is used (avoids NVIDIA/CUDA transitive deps).

# Core
boto3>=1.34.0
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0

# NLP & Embeddings  (torch pulled from CPU index above)
sentence-transformers>=2.2.2
tiktoken>=0.5.0
defusedxml>=0.7.1

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# API
fastapi>=0.109.0
mangum>=0.17.0
slowapi>=0.1.9

# AWS Lambda packaging
aws-lambda-powertools>=2.30.0
