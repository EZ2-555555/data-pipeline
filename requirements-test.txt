# Lightweight requirements for CI lint + unit tests.
# fastembed uses ONNX Runtime (~50 MB) — no PyTorch needed.

# Testing & Linting
pytest>=7.4.0
pytest-cov>=4.1.0
ruff>=0.3.0

# Core
boto3>=1.34.0
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0
defusedxml>=0.7.1

# Embeddings (ONNX-based)
fastembed>=0.4.0

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# API
fastapi>=0.109.0
httpx>=0.27.0
uvicorn>=0.27.0
mangum>=0.17.0
slowapi>=0.1.9

# NLP (lightweight — tokenizer only)
tiktoken>=0.5.0
rank-bm25>=0.2.2
pytest-cov>=4.1.0
