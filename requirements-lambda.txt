# Lightweight runtime dependencies for Lambda function code.
# Heavy ML deps (torch, sentence-transformers, tiktoken) are in the Lambda Layer.
# boto3 is provided by the Lambda runtime — do not bundle it.

# Core
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0
defusedxml>=0.7.1

# Database
psycopg2-binary>=2.9.9
pgvector>=0.2.4

# API
fastapi>=0.109.0
mangum>=0.17.0
slowapi>=0.1.9

# AWS Lambda packaging
aws-lambda-powertools>=2.30.0
