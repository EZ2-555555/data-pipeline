\chapter{Experimental Plan and Evaluation Metrics}

\section{Evaluation Objective}
\label{sec:eval_objective}

The evaluation operationalises the constrained optimisation objective stated in Equation~\ref{eq:opt} (Chapter~3) and the formal hypotheses H\textsubscript{0}/H\textsubscript{1} (Section~\ref{sec:method_overview}). It aims to answer three questions, each corresponding to a subset of the research questions mapped in Table~\ref{tab:rq_map}: (1) Does hybrid retrieval achieve statistically higher contextual precision and grounding faithfulness than vector-only retrieval (RQ2)? (2) What latency and cost overhead does hybrid retrieval introduce, and does the optimal weight configuration sit within the latency constraint (RQ3)? (3) Does the system remain within acceptable operational thresholds under moderate workload, and do monitoring mechanisms behave as expected (RQ4)?

Statistical significance of mean latency, citation grounding, and context precision differences between baseline and hybrid configurations is assessed using a one-sided Wilcoxon signed-rank test ($\alpha = 0.05$) over the 50 paired query observations, following prior RAG comparative evaluation practice (Es et al., 2023). The statistical analysis is supplemented with Cohen's $d$ effect size and bootstrapped 95\% confidence intervals (1{,}000 resamples) to quantify practical significance beyond $p$-value testing. If H\textsubscript{0} cannot be rejected at the 0.05 level, the evaluation characterises the effect size and identifies corpus or workload conditions under which hybrid retrieval provides practical benefit independent of statistical significance.

\section{Experimental Configurations}
\label{sec:eval_configs}

Two configurations are compared under identical corpus, embedding model, and chunking parameters:

\begin{table}[H]
\caption{Experimental Configuration Comparison.}
\label{tab:exp_configs}
\centering
\begin{tabular}{p{3.5cm}cc}
\toprule
\textbf{Component} & \textbf{Baseline (Vector-Only)} & \textbf{Hybrid (Implemented)} \\
\midrule
Metadata Filtering & $\times$ & \checkmark \\
Vector Similarity & \checkmark & \checkmark \\
Reranking-Lite & $\times$ & \checkmark \\
Recency Weighting & $\times$ & \checkmark \\
Top-$k$ returned & 5 & 5$^{\dagger}$ \\
Embedding Model & MiniLM (384-dim) & MiniLM (384-dim) \\
Cost Governance & None & Budget guard \\
\bottomrule
\end{tabular}
\medskip
\noindent\small{$^{\dagger}$Hybrid retrieval over-retrieves $4 \times k = 20$ candidates at the vector search stage before reranking and final selection of top-5 (see Section~\ref{sec:hybrid}).}
\end{table}

\section{Evaluation Dataset}
\label{sec:eval_dataset}

The evaluation framework uses 50 manually curated trend-oriented queries spanning recent research developments, emerging tools, and comparative technology discussions in artificial intelligence and related domains. Queries are categorized into three types---\textit{research\_trend} (18 queries), \textit{tool\_technology} (16 queries), and \textit{comparative} (16 queries)---and include ground-truth reference answers for automated scoring.

Example queries include: ``What are recent approaches to efficient transformer architectures?''; ``What emerging AI tools are currently being discussed by developers?''; ``How does RAG compare to fine-tuning for domain-specific applications?''; and ``What are recent trends in large language model compression?''

\textbf{Justification for evaluation set size.} The evaluation objective is a controlled comparison between two retrieval configurations under identical corpus and embedding conditions. A paired comparison across 50 carefully constructed queries with ground-truth annotations provides sufficient statistical power to detect meaningful latency, citation grounding, and context precision differences with the Wilcoxon signed-rank test, and exceeds the evaluation scale adopted in prior RAG evaluation work (Es et al., 2023; Saad-Falcon et al., 2023). Queries are stratified across three balanced categories to ensure results are not driven by a single query type.

\section{Evaluation Metrics}
\label{sec:eval_metrics}

Figure~\ref{fig:eval_framework} summarizes the implemented evaluation framework.

% ---- TikZ: Evaluation framework diagram ----
\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}

% Metric boxes (top row)
\node[evalbox=awsblue] (prec) at (-6.0,0)
  {\textbf{Context Precision}\\[2pt]\scriptsize Target: $> 0.75$\\RQ2};

\node[evalbox=awsgreen] (faith) at (-2.0,0)
  {\textbf{Faithfulness}\\[2pt]\scriptsize Target: $\geq 0.80$\\RQ2};

\node[evalbox=awsorange] (lat) at (2.0,0)
  {\textbf{p95 Latency}\\[2pt]\scriptsize Target: $< 2.0$s (retrieval)\\RQ3};

\node[evalbox=awspurple] (cost) at (6.0,0)
  {\textbf{Cost / Month}\\[2pt]\scriptsize Target: $<$ USD~15\\at 1,500 queries/mo\\RQ3};

% Measurement method boxes (bottom row — sources of the metric)
\node[rectangle, draw=awsgray!40, fill=sectionbg, rounded corners=3pt,
      font=\scriptsize, minimum height=0.65cm, text width=3.2cm, text centered]
  (m1) at (-6.0,-1.9) {Measurement:\\RAGAS ContextPrecision\\(Groq judge)};

\node[rectangle, draw=awsgray!40, fill=sectionbg, rounded corners=3pt,
      font=\scriptsize, minimum height=0.65cm, text width=3.2cm, text centered]
  (m2) at (-2.0,-1.9) {Measurement:\\RAGAS Faithfulness\\+ citation ratio};

\node[rectangle, draw=awsgray!40, fill=sectionbg, rounded corners=3pt,
      font=\scriptsize, minimum height=0.65cm, text width=3.2cm, text centered]
  (m3) at (2.0,-1.9) {Measurement:\\Per-query timing\\+ CloudWatch Logs};

\node[rectangle, draw=awsgray!40, fill=sectionbg, rounded corners=3pt,
      font=\scriptsize, minimum height=0.65cm, text width=3.2cm, text centered]
  (m4) at (6.0,-1.9) {Measurement:\\Token count\\$\times$ per-token pricing};

% Arrows point UP: measurement method produces the metric
\draw[parrow] (m1) -- (prec);
\draw[parrow] (m2) -- (faith);
\draw[parrow] (m3) -- (lat);
\draw[parrow] (m4) -- (cost);

% Statistical test annotation — spans all four metric columns
\node[rectangle, draw=propblue!60, fill=propblue!10, rounded corners=3pt,
      font=\scriptsize, text width=13.5cm, text centered, inner sep=3pt]
  (stat) at (0, -3.5) {Statistical test: Wilcoxon signed-rank (one-sided, $\alpha{=}0.05$) applied to paired latency and citation grounding differences ($N{=}50$) \enspace$|$\enspace Cohen's $d$ effect size \enspace$|$\enspace Bootstrap 95\% CI};
\draw[-{Stealth}, dashed, color=propblue!60] (stat.north -| m3.south) -- (m3.south);
\draw[-{Stealth}, dashed, color=propblue!60] (stat.north -| m2.south) -- (m2.south);

\end{tikzpicture}%
}
\caption{Evaluation framework: four measurement dimensions with target thresholds, mapped to research questions.}
\label{fig:eval_framework}
\end{figure}

\subsection{Context Precision}

Retrieval precision is measured using RAGAS \texttt{ContextPrecisionWithoutReference}, which evaluates whether retrieved documents are relevant to the query using automated LLM-based scoring (with Groq \texttt{llama-3.3-70b-versatile} as the evaluation judge). To avoid same-model bias, the generation model (\texttt{llama-3.1-8b-instant}) is deliberately different from the RAGAS judge; a commercial judge (e.g.\ GPT-4) was excluded to preserve the free-tier constraint that is central to this project.

\subsection{Faithfulness and Hallucination Verification}

Faithfulness is assessed through two complementary mechanisms: (1) RAGAS faithfulness scoring, which decomposes responses into atomic claims and verifies each against retrieved context (target: $\geq 0.80$); and (2) runtime citation grounding ratio, measuring the fraction of response sentences containing valid \texttt{[Source N]} citations (target: $> 0.80$). Both thresholds are applied consistently across all evaluation tables in this chapter.

\subsection{Latency}

End-to-end latency is measured per query, with aggregate statistics computed: mean, median, p95, maximum, and minimum. Latency is decomposed by retrieval mode and query category. The target threshold is p95 $< 2$ seconds for the retrieval component (excluding LLM generation time, which varies by backend).

\subsection{Cost Per Query}

Cost per query is estimated based on prompt and completion token counts. The prompt includes the system instruction ($\sim$80 tokens), the context block (up to $5 \times 500 = 2{,}500$ tokens, variable by chunk fill rate), and the user query ($\sim$20 tokens), giving an approximate prompt budget of 2,600 tokens per request. The completion target is capped at 300 tokens. Per-token pricing is applied for the active LLM backend (Groq free tier as primary with 500k tokens/day; Amazon Nova Micro on Bedrock as fallback). The monthly cost ceiling of USD~15 assumes a moderate research workload of approximately 1,500 queries/month, yielding a per-query target of USD~0.01.

\subsection{Reranking Weight Optimization}

Grid search is performed over the reranking weight space to identify the optimal configuration:

\begin{itemize}[leftmargin=0.5in]
  \item $\alpha \in \{0.4, 0.5, 0.6, 0.7\}$ (semantic similarity weight)
  \item $\beta = \gamma = (1 - \alpha) / 2$ (equal keyword overlap and recency weights)
  \item Constraint: p95 latency $\leq 2.0$ seconds
  \item Objective: maximize mean context precision
\end{itemize}

The configuration yielding the highest mean precision without violating the latency threshold is selected as the production weight configuration.

\subsection{Sensitivity Analysis}
\label{sec:sensitivity}

To justify reranking weight selection beyond grid search, a one-at-a-time (OAT) sensitivity analysis sweeps each parameter ($\alpha$, $\beta$, $\gamma$) independently through $[0.0, 0.1, 0.2, \ldots, 1.0]$ in 11 steps while redistributing the remaining weight proportionally between the other two parameters. This produces 33 evaluation runs and reveals the marginal influence of each parameter on mean similarity and p95 latency, providing empirical evidence for weight selection rather than relying solely on grid search or intuition.

\subsection{Statistical Tests for Baseline vs.\ Hybrid Comparison}
\label{sec:stat_tests}

The baseline-vs-hybrid comparison employs three complementary statistical analyses:

\begin{enumerate}[leftmargin=0.5in]
  \item \textbf{Wilcoxon signed-rank test} (one-sided, $\alpha = 0.05$ and $\alpha = 0.01$) applied to paired latency and citation grounding differences across the 50 query pairs. This non-parametric test is appropriate for the expected non-normal distribution of latency and retrieval quality data.
  \item \textbf{Cohen's $d$ effect size} computed as $d = \bar{x}_{\text{diff}} / s_{\text{diff}}$, where $\bar{x}_{\text{diff}}$ is the mean paired difference and $s_{\text{diff}}$ is its standard deviation. Effect sizes are classified as negligible ($< 0.2$), small ($\geq 0.2$), medium ($\geq 0.5$), or large ($\geq 0.8$).
  \item \textbf{Bootstrap 95\% confidence interval} (1{,}000 resamples with replacement) of the mean latency difference. The 2.5th and 97.5th percentiles provide a model-free estimate of uncertainty that is robust to outliers.
\end{enumerate}

\subsection{Citation Grounding with Partial Relevance}
\label{sec:partial_relevance}

Citation grounding is computed using a weighted relevance scoring function that distinguishes three categories for each citation:

\begin{itemize}[leftmargin=0.5in]
  \item \textbf{Fully Relevant:} keyword overlap between query tokens and cited chunk tokens $\geq 0.5$
  \item \textbf{Partially Relevant:} $0 < \text{overlap} < 0.5$
  \item \textbf{Invalid:} out-of-bounds citation index (references a non-existent source)
\end{itemize}

\noindent The function returns a decomposed grounding report including \texttt{grounding\_ratio} (fraction of valid citations), \texttt{relevance\_score} (mean overlap), and per-class counts (\texttt{full\_count}, \texttt{partial\_count}, \texttt{invalid\_count}). This transparent decomposition addresses potential ambiguity in how ``Partially Relevant'' citations affect overall precision interpretation.

\subsection{Composite Performance Metric}
\label{sec:composite}

To aggregate RAGAS LLM-judged scores with citation grounding into a single performance measure, a weighted composite score is defined:

\begin{equation}
  \text{Composite} = 0.35 \times F + 0.25 \times R + 0.20 \times P + 0.20 \times C
  \label{eq:composite}
\end{equation}

\noindent where $F$ = Faithfulness, $R$ = AnswerRelevancy, $P$ = ContextPrecision, and $C$ = CitationGrounding.

\noindent The weight allocation reflects the primacy of factual correctness (faithfulness, 0.35), followed by answer quality (relevancy, 0.25), and equal contributions from retrieval performance (precision, 0.20) and the system's citation value-add (grounding, 0.20). This composite metric enables direct comparison between baseline and hybrid configurations on a single interpretable scale.

\subsection{Drift Detector Validation}
\label{sec:drift_validation}

To demonstrate that the drift detection mechanism (Section~\ref{sec:drift}) correctly identifies retrieval quality degradation, five simulated scenarios are evaluated:

\begin{enumerate}[leftmargin=0.5in]
  \item \textbf{Healthy baseline} --- establishes baseline from probe query results; verifies no false alarm
  \item \textbf{18\% quality degradation} --- simulated via $0.82\times$ scaling of baseline similarities; verifies threshold detection triggers an alert
  \item \textbf{4\% normal fluctuation} --- simulated via $0.96\times$ scaling ($\pm 4\%$ dip); verifies specificity (no false alarm under minor variation)
  \item \textbf{Shewhart breach without threshold breach} --- current similarity within the 10\% threshold but below the Shewhart $3\sigma$ lower control limit; verifies that statistical process control detects subtle degradation missed by the fixed threshold
  \item \textbf{Catastrophic failure} --- $>90\%$ quality collapse ($0.85 \to 0.05$); verifies both threshold and Shewhart detection mechanisms fire
\end{enumerate}

\noindent Scenarios 1, 3, and the first-run case (no prior history $\to$ baseline creation without alerting, tested in \texttt{test\_drift\_first\_run\_establishes\_baseline}) validate specificity (no false alarms), while scenarios 2, 4, and 5 validate sensitivity (true positives under degradation).

\textbf{Drift-to-quality impact assessment.} To demonstrate that drift detection improves retrieval quality rather than merely generating operational alarms, the evaluation includes a before/after remediation comparison. When a drift alert fires during the evaluation phase, context precision and faithfulness are measured on the probe query set immediately before remediation (corpus audit and metadata filter re-calibration) and again after remediation is applied. The paired difference in mean context precision quantifies the retrieval quality gain attributable to drift-triggered intervention. Additionally, a counterfactual analysis compares precision trajectories with and without drift-triggered remediation across successive ingestion cycles: the drift-enabled configuration applies corrective action upon alert, while the drift-disabled baseline continues ingesting without intervention. A statistically significant precision divergence (Wilcoxon, $\alpha = 0.05$) between the two trajectories would provide evidence that the drift mechanism contributes to sustained retrieval quality, not just operational visibility.

\subsection{Monthly Cost Projection}
\label{sec:cost_projection}

Cost feasibility is validated by measuring per-query token consumption (prompt + completion) during the evaluation phase and projecting monthly costs for three workload scenarios: 50, 100, and 200 queries/day. The projection includes LLM inference costs (Groq free tier: 500{,}000 tokens/day; Bedrock: pay-per-token) and AWS infrastructure (RDS db.t3.micro, Lambda, S3, SQS within free-tier ceilings). A feasibility assessment flags whether any scenario exceeds the USD~15/month budget target.

\begin{table}[H]
\caption{Summary of Evaluation Metrics and Targets.}
\label{tab:eval_targets}
\centering
\begin{tabular}{p{3.0cm}p{3.0cm}p{3.2cm}p{3.0cm}}
\toprule
\textbf{Metric} & \textbf{Dimension} & \textbf{Method} & \textbf{Target} \\
\midrule
Context Precision & Retrieval quality & RAGAS ContextPrecision (Groq judge) & $> 0.75$ \\
Faithfulness & Grounding quality & RAGAS Faithfulness score & $\geq 0.80$ \\
Citation Grounding Ratio & Hallucination proxy & Weighted partial-relevance analysis & $> 0.80$ \\
Composite Score & Overall quality & $0.35F + 0.25R + 0.20P + 0.20C$ & --- \\
p95 Latency & System performance & Per-query timing (retrieval stage) & $< 2.0$ seconds \\
Cost per Month & Operational cost & Token usage $\times$ per-token pricing & $<$~USD 15 at 1,500 q/mo \\
Wilcoxon $p$-value & Statistical significance & Paired signed-rank test & $< 0.05$ \\
Cohen's $d$ & Effect size & Paired differences & $\geq 0.5$ (medium) \\
Bootstrap 95\% CI & Uncertainty & 1{,}000 resamples & Does not span zero \\
\bottomrule
\end{tabular}
\end{table}

\section{Analysis and Discussion Framework}
\label{sec:discussion}

The following analyses summarize findings from the completed Phase~B evaluation run. Targets listed in Table~\ref{tab:eval_targets} are design-time thresholds derived from system requirements and prior RAG benchmarks.

\textbf{Precision vs.\ computational overhead.} Context precision is comparable between baseline (0.340) and hybrid (0.323) configurations, with no statistically significant difference (Wilcoxon $p = 0.906$). Hybrid retrieval adds 2.18\,s mean latency overhead, primarily in the reranking stage. The Wilcoxon test on latency shows significance at $p = 0.012$ but with only a small effect size (Cohen's $d = -0.28$).

\textbf{Retrieval strictness vs.\ recall.} The 180-day metadata filter and minimum similarity threshold (0.15) reduce retrieval noise but may exclude relevant older documents. The source distribution (Figure~\ref{fig:viz_sources}) shows that DevTo and ArXiv dominate both modes, with hybrid retrieval concentrating sources slightly further.

\textbf{Cost--performance balance.} Token usage is comparable between modes (2{,}363 vs.\ 2{,}502 tokens/query), and monthly cost projections remain well under USD~15 for realistic workloads (Table~\ref{tab:viz_dashboard}). At 100 queries/day, projected monthly LLM cost is USD~0.35.

\textbf{Reranking weight sensitivity.} Grid search identifies $\alpha = 0.70$, $\beta = 0.15$, $\gamma = 0.15$ as optimal (Figure~\ref{fig:viz_pareto}). Sensitivity analysis confirms that similarity weight ($\alpha$) has the strongest marginal influence on mean similarity, while BM25 ($\beta$) and recency ($\gamma$) contribute diminishing returns above 0.15.

\textbf{Production suitability.} Three of four primary targets are met: citation grounding exceeds 0.80, retrieval p95 latency (1.29\,s) is well under 2.0\,s, and monthly cost remains below USD~15. Context precision (0.34) and faithfulness (0.16) fall short of original thresholds---a limitation attributable to the 8B-parameter generation model's tendency to extrapolate beyond context.

\section{Result Visualizations}
\label{sec:visualizations}

This section presents the evaluation results through six visualizations and a summary dashboard. All data were collected from a 50-query evaluation run executed on 28~March~2026 against the production database (3{,}711 indexed documents, 5{,}769 chunks).

\subsection{Baseline vs.\ Hybrid Retrieval Comparison}

Figure~\ref{fig:viz_comparison} presents a grouped bar chart comparing baseline and hybrid retrieval across four core evaluation metrics: context precision, faithfulness, answer relevancy, and citation grounding ratio. This visualization provides the primary evidence for answering Research Question~2 (Section~\ref{sec:rq}).

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{comparison_bar.png}
\caption{Baseline vs.\ hybrid retrieval comparison across four evaluation dimensions.}
\label{fig:viz_comparison}
\end{figure}

\subsection{Grid Search: Precision--Latency Pareto Frontier}

Figure~\ref{fig:viz_pareto} presents the grid search results as a dual-axis plot with the reranking similarity weight ($\alpha$) on the x-axis, mean similarity on the left y-axis, and p95 latency on the right y-axis. The best configuration ($\alpha = 0.70$, $\beta = 0.15$, $\gamma = 0.15$) achieves the highest similarity (0.526) while keeping p95 latency (1.29\,s) well below the 2.0\,s threshold.

\begin{figure}[H]
\centering
\includegraphics[width=0.88\textwidth]{grid_search_pareto.png}
\caption{Grid search: reranking weight ($\alpha$) vs.\ mean similarity and p95 latency.}
\label{fig:viz_pareto}
\end{figure}

\subsection{Per-Source Retrieval Distribution}

Figure~\ref{fig:viz_sources} presents a stacked bar chart showing the distribution of retrieved documents by source across baseline and hybrid retrieval modes. DevTo articles dominate both modes ($\approx$55--58\%), with arXiv second ($\approx$37--38\%). Hybrid retrieval slightly concentrates sources, reducing Hacker News and RSS contributions.

\begin{figure}[H]
\centering
\includegraphics[width=0.72\textwidth]{source_distribution.png}
\caption{Per-source retrieval distribution for baseline vs.\ hybrid retrieval modes.}
\label{fig:viz_sources}
\end{figure}

\subsection{Latency by Query Category}

Figure~\ref{fig:viz_latency} presents a horizontal grouped bar chart comparing mean end-to-end latency across query categories. Research-trend queries are fastest for hybrid retrieval (5.78\,s), while tool/technology queries incur the highest latency in both modes due to longer generated responses.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{latency_by_category.png}
\caption{Mean end-to-end latency by query category for baseline and hybrid retrieval.}
\label{fig:viz_latency}
\end{figure}

\subsection{Multi-Metric Radar Comparison}

Figure~\ref{fig:viz_radar} presents a radar chart overlaying baseline and hybrid retrieval across five evaluation dimensions---context precision, faithfulness, answer relevancy, citation grounding, and composite score---on a single polar plot. The near-identical silhouettes confirm that hybrid retrieval preserves baseline quality across all metrics, with citation grounding showing the only notable improvement (+0.04).

\begin{figure}[H]
\centering
\includegraphics[width=0.58\textwidth]{radar_comparison.png}
\caption{Multi-metric radar comparison of baseline and hybrid retrieval.}
\label{fig:viz_radar}
\end{figure}

\subsection{Per-Query Latency Shift}

Figure~\ref{fig:viz_paired_diff} shows the distribution of per-query latency differences (hybrid $-$ baseline). Of the 50 queries, 16 completed faster under hybrid retrieval while 34 were slower, with a mean overhead of $+2.18$\,s. The histogram reveals that most slower queries cluster in a narrow $+2$--$12$\,s band, while a small subset of queries benefit from hybrid retrieval's ability to surface higher-ranked results more efficiently.

\begin{figure}[H]
\centering
\includegraphics[width=0.82\textwidth]{latency_paired_diff.png}
\caption{Paired-difference histogram of per-query latency shift (hybrid $-$ baseline).}
\label{fig:viz_paired_diff}
\end{figure}

\subsection{Evaluation Summary Dashboard}

Table~\ref{tab:viz_dashboard} serves as the primary summary dashboard, consolidating all evaluation metrics into a single comparison table with target thresholds and pass/fail status indicators.

\begin{table}[H]
\caption{Evaluation Summary Dashboard: Baseline vs.\ Hybrid with Target Thresholds.}
\label{tab:viz_dashboard}
\centering
\begin{tabular}{p{3.5cm}p{2.0cm}p{2.0cm}p{2.0cm}p{2.5cm}}
\toprule
\textbf{Metric} & \textbf{Target} & \textbf{Baseline} & \textbf{Hybrid} & \textbf{Status} \\
\midrule
Context Precision & $> 0.75$ & 0.340 & 0.323 & \textcolor{propred}{\ding{55} Miss} \\
Faithfulness (RAGAS) & $\geq 0.80$ & 0.156 & 0.126 & \textcolor{propred}{\ding{55} Miss} \\
Answer Relevancy & --- & 0.943 & 0.927 & --- \\
Citation Grounding & $> 0.80$ & 0.880 & 0.920 & \textcolor{awsgreen}{\ding{51} Pass} \\
Composite Score & --- & 0.534 & 0.525 & --- \\
p95 Latency (retr.) & $< 2.0$s & 1.29\,s & 1.29\,s & \textcolor{awsgreen}{\ding{51} Pass} \\
Mean Latency (e2e) & --- & 8.16\,s & 10.33\,s & --- \\
Cost / Month (100 q/d) & $<$ USD 15 & \$0.33 & \$0.35 & \textcolor{awsgreen}{\ding{51} Pass} \\
Optimal $\alpha$ & --- & N/A & 0.70 & --- \\
Wilcoxon $p$ (latency) & $< 0.05$ & \multicolumn{2}{c}{$p = 0.012$} & \textcolor{awsgreen}{\ding{51} Sig.} \\
Cohen's $d$ & $\geq 0.5$ & \multicolumn{2}{c}{$d = -0.28$} & \textcolor{propred}{\ding{55} Small} \\
95\% CI (latency diff) & excl.\ zero & \multicolumn{2}{c}{$[-4.29, 0.06]$} & \textcolor{propred}{\ding{55} Incl.\ 0} \\
Drift Accuracy & 100\% & \multicolumn{2}{c}{75\% (3/4)} & \textcolor{propred}{\ding{55} Miss} \\
\bottomrule
\end{tabular}
\end{table}

\medskip
\noindent This chapter defined the nine-phase evaluation framework with explicit quantitative targets and presented results from the 50-query evaluation run. The dashboard (Table~\ref{tab:viz_dashboard}) shows that three of four primary targets are met: citation grounding exceeds 0.80, retrieval p95 latency is well under 2.0\,s, and monthly cost remains below USD~15. Context precision (0.34) and faithfulness (0.16) fall short of original thresholds, a limitation attributable to the 8B-parameter generation model and discussed further in Chapter~6. Chapter~6 documents implementation status and engineering decisions.
